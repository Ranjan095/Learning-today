/** @format */

import React from "react";
let initilaState = {
  isLoading: false,
  isSuccess: true,
  isError: false,
};
export const reducer = (state = initilaState, { type, payload }) => {
  switch (type) {
    default: {
      return state;
    }
  }
};
